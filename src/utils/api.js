import request from './request';

export async function queryNb() {
  console.log('111');
  // const url = `https://api.github.com/search/repositories?q=stars:>1+language:${language}&sort=stars&order=desc&type=Repositories&page=${page}`;
  // return request(url);
}
